config:
  target: "http://localhost:3004"
  phases:
    - duration: 600
      arrivalRate: 360
scenarios:
  - flow:
      - get: 
          url: '/'
        count: 2
      - get:
          url: '/getImages/{{ $randomNumber(1,9990000) }}'
        count: 2
      - get:
          url: '/getRooms/{{ $randomNumber(1,1000000) }}'
        count: 2

 